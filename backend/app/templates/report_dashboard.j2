{# AI 半导体产业链盯盘仪表盘报告模板 #}
🎯 {{ report_date }} AI 半导体产业链盯盘
━━━━━━━━━━━━━━━━━━━━
📡 监控 {{ total_tickers }} 只 | {{ regime_label }}
{% if signal_counts %}🟢强势:{{ signal_counts.buy }}  🟡持有:{{ signal_counts.hold }}  🔴避开:{{ signal_counts.avoid }}{% endif %}

📊 核心标的
(RSI>70过热 | RSI<30超卖 | ⬇支撑 ⬆压力)
┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
{% for item in top_movers -%}
{{ item.emoji }} {{ item.ticker }}  ${{ item.price }}  {{ item.pct_change }}%  RSI{{ item.rsi }}  {{ item.trend_label }}
{%- if item.support %}  ⬇${{ item.support }}{% endif %}{% if item.resistance %}  ⬆${{ item.resistance }}{% endif %}

{% endfor -%}

{% if risk_alerts -%}
🚨 风险警报
{% for alert in risk_alerts -%}
• {{ alert }}
{% endfor %}
{% endif -%}

{% if catalysts -%}
✨ 催化因素
{% for cat in catalysts -%}
• {{ cat }}
{% endfor %}
{% endif -%}

📈 板块强弱
┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
{{ bucket_lines }}

🎯 操作建议
┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
{% if add_candidates -%}
可关注加仓: {{ add_candidates | join(' / ') }}
{% endif -%}
{% if avoid_tickers -%}
暂时回避: {{ avoid_tickers | join(' / ') }}
{% endif -%}
结论: {{ action_suggestion }}
新买入上限: ${{ new_buy_cap }}

{% if sleep_orders -%}
😴 睡觉挂单建议
┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄
{% for order in sleep_orders -%}
• {{ order.ticker }}({{ order.bucket_label }}): ${{ order.low }}-${{ order.high }}, ≤${{ order.max_dollars }}
{% endfor %}
{% endif -%}

⚠️ 本报告由本地技术指标+AI分析生成，不构成投资建议。
━━━━━━━━━━━━━━━━━━━━
